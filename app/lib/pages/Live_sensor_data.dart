import 'package:flutter/material.dart';

class LiveSensorData extends StatelessWidget {
  const LiveSensorData({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Live Sensor Data', style: TextStyle(fontSize: 24)),
    );
  }
}
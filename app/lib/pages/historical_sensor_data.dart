import 'package:flutter/material.dart';

class HistoricalSensorData extends StatelessWidget {
  const HistoricalSensorData({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Historical Sensor Data', style: TextStyle(fontSize: 24)),
    );
  }
}